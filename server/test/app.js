var app = require('../app');

describe('Creating the express app', () => {

    it('should create an app and start the server', (done) => {

        console.log('initial setup');
        app;
        done();

    });
})